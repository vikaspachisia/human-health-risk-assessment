'use strict'

const servers = [
    ['web-gateway', { hostname: 'localhost', port: 3030 }],
    ['mobile-gateway', { hostname: 'localhost', port: 3032 }],
    ['api-gateway', { hostname: 'localhost', port: 3034 }]
];